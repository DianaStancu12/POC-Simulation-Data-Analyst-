2. Data analysis and QC

I identified several instances of "column shifting" in the raw CSV. This was caused by semicolons (;). 
We should implement a validation step to flag logical inconsistencies, such as entities reporting millions in revenue but having zero employees. These entries should be isolated for verification to ensure the financial data is reliable for procurement risk analysis.


3. Summarize your work

Instead of manual verification, I built a Weighted Scoring Model in Excel to objectively rank the 5 candidates provided for each row. I assigned weights based on the reliability of each data attribute:

- I assigned 1.0 point for an exact name match and 0.5 points for a partial string match (using the SEARCH function).

- I added 0.3 points for a country-level match and 0.1 points for a city-level match to differentiate between global branches.
- I calculated a TOTAL score for every candidate.

The Exact Formulas Used:

Same Country: =IF([@[input_main_country_code]]=[@[main_country_code]]; 0.3; 0)

Contain Name: =IF(ISNUMBER(SEARCH([@[input_company_name]]; [@[company_name]])); 0.5; 0)

Same Name: =IF([@[input_company_name]]=[@[company_name]]; 1; 0)

Same City: =IF([@[input_main_city]]=[@[main_city]]; 0.1; 0)

Total Score: =SUM([@[same_country]]; [@[contain_name]]; [@[same_name]]; [@[same_city]])


For the majority of rows where one candidate had a significantly higher score than the others (e.g., a score of 1.5 or 1.8), I used an automated sorting and deduplication process to keep only the top-ranked match.

For cases where multiple candidates shared the same low score (e.g., all 5 candidates scoring 0.3 or 0.4), I did not rely on automation. Instead:
- I manually inspected these specific rows to compare the input against the candidate's legal names or descriptions to identify the correct entity.
- If a clear match was found during this review, I kept the most accurate one.
- If none of the candidates met the quality criteria or if the data remained too ambiguous, I chose to leave the entry unallocated.